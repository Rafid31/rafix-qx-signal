[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r trader_requirements.txt && uvicorn trader:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
